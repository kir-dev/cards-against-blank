<%= form_with(model: white_card, local: true) do |form| %>
  <% if white_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(white_card.errors.count, "error") %> prohibited this white_card from being saved:</h2>

      <ul>
      <% white_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
