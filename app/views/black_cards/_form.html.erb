<%= form_with(model: black_card, local: true) do |form| %>
  <% if black_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(black_card.errors.count, "error") %> prohibited this black_card from being saved:</h2>

      <ul>
      <% black_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field">
    <%= form.label :pick %>
    <%= form.number_field :pick %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
